# Copy to .env and fill with your Supabase project values (Project Settings -> API).
# The anon/publishable key is PUBLIC (it ships in the browser bundle) — safe here.
# The service-role / secret key must NEVER be put here or committed.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
